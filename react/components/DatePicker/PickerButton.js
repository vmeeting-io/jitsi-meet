import Button from '@atlaskit/button/standard-button';
import * as React from 'react';

export default function PickerButton(props) {
  return <Button appearance="primary" type="button" {...props} />;
}
